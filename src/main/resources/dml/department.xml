<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="department">
	
	<!-- 20170516 김민지 직무 리스트 가져오기-->
	<select id="getDepartment" parameterType="String" resultType="com.toy.project.model.DepartmentDto">
		SELECT department.* 
			FROM department
	</select>
</mapper>